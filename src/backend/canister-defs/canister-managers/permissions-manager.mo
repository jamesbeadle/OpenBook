import T "../../data-types/types";

module {

  public class PermissionsManager() {
    public func hasPermission(principalId: T.PrincipalId, permission: T.AccountancyPermissions) : Bool {
        return false; //TODO
    };

  }
};